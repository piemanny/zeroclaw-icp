[package]
name = "vault"
version = "0.1.0"
edition = "2021"

[dependencies]
ic-cdk = "0.17"
ic-stable-structures = "0.6"
candid = "0.10"
serde = { version = "1", features = ["derive"] }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
strip = true