[package]
name = "vault"
version = "0.1.0"
edition = "2021"

[dependencies]
ic-cdk = "0.20"
ic-stable-structures = "0.7"
candid = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
strip = true